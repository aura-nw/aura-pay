enum AppNetwork{
  auraEvm,
  auraCosmos,
  custom,
}

enum AccountCreateType{
  normal,
  google,
  import,
}

enum AccountType{
  normal,
  abstraction,
}

enum ControllerKeyType{
  passPhrase,
  privateKey,
}