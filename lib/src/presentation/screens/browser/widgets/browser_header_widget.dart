import 'package:flutter/material.dart';

class BrowserHeaderWidget extends StatelessWidget {
  const BrowserHeaderWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [
        Expanded(
          child: SizedBox(),
        ),

      ],
    );
  }
}
