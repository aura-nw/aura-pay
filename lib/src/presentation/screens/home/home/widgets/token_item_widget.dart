import 'package:flutter/material.dart';

class TokenItemWidget extends StatelessWidget {
  const TokenItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
