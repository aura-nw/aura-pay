import 'package:flutter/material.dart';

class ReceiveTokenWidget extends StatelessWidget {
  const ReceiveTokenWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [

      ],
    );
  }
}
