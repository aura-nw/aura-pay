enum OnBoardingChoiceOption{
  createAccount,
  importAccount,
  recoverAccount,
}

enum ChoiceModalType{
  single,
  multi,
}

enum ChoiceModalSize{
  small,
  medium,
  high,
}

enum ImportWalletType{
  privateKey,
  passPhrase,
}
